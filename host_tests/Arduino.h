#pragma once
#include <cstdint>
#include <cstring>
#include <algorithm>
#include <iostream>

using std::min;
using std::max;

#define constrain(amt,low,high) ((amt)<(low)?(low):((amt)>(high)?(high):(amt)))
